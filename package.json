{
  "name": "animaid",
  "version": "2.0.0",
  "description": "Procedural Skeletal Animation Generator from ULID",
  "main": "animaid.js",
  "scripts": {
    "build": "esbuild bpm-detectors.js --bundle --outfile=bpm-detectors-bundle.js --format=iife",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/norn/animaid.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "realtime-bpm-analyzer": "^5.0.15",
    "web-audio-beat-detector": "^8.2.37"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "esbuild": "^0.28.1",
    "events": "^3.3.0"
  }
}
